<!DOCTYPE html>
<html>

<head>
    <title>Page Title</title>
    <link rel="stylesheet" href="/assets/webui/css/epg.css" />
    <script src="/assets/js/aminopvr.js"></script>
    <script src="/assets/webui/js/ui.js"></script>
</head>

<body>

<table width="100%" border="0" cellpadding="4" cellspacing="2" class="list small">
#set $channelIndex = 0
#for $channel in $channels
#if $channelIndex % 10 == 0
<tr>
    <td class="menu" align="right">&lt;</td>
    #for $timeBlock in $timeBlocks
    <td class="menu nowrap" colspan="$timeBlock.blocks" style="width: $timeBlock.percentage%" align="center">$timeBlock.time</td>
    #end for
    <td class="menu">&gt;</td>
</tr>
#end if
<tr>
    <td class="x-channel"><img src="$channel.logo_path" title="$channel.number - $channel.name"/><br/>$channel.number</td>
    #for $program in $channel.programs
    <td class="small cat_$program.category" colspan="$program.blocks" style="width: $program.percentage%" valign="top">
        #if 'subtitle' in $program
        $program.title: <br/>$program.subtitle
        #else
        $program.title
        #end if
    </td>
    #end for
    <td>&nbsp;</td>
</tr>
#set $channelIndex = $channelIndex + 1
#end for
</table>

</body>
</html>
